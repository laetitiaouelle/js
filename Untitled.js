'use strict'

/*
 * Create a getLast function that takes an array or a string
 * and return the last element.
 */

// Your code
const getLast = oup => oup[ getLast.length - 1]
console.log(getLast('abcdefgh'))
// examples of tests :
const assert = require('assert')